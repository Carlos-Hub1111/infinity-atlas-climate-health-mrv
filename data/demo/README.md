# Demo data

Sprint 0 demo data is synthetic.

Rules:

- no identifiable child data;
- no clinical records;
- no confidential UNICEF documents;
- no sensitive community data;
- every generated record must be marked with `is_synthetic=true`.

The seed script lives in:

```text
backend/app/seed.py
```
